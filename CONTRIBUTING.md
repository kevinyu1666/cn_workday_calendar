# Contributing

这个项目维护中国大陆全国性节假日与调休补班数据。数据必须可追溯，代码必须可读，不能为了少写几行牺牲审计能力。

## 数据更新规则

1. 只接受国务院办公厅正式通知，或明确转载自中国政府网的权威部门/媒体页面。
2. 每次新增年份时，同步更新 `data/holiday_schedules.json` 和 Dart 内置数据。
3. 每个调休补班日都必须写入对应节日的 `adjustedWorkdays`。
4. 合并假期需要保留所有相关节日 ID，展示名可以使用合并名称。
5. 新增数据必须补测试，至少覆盖一个放假日和一个调休补班日。

## 代码规则

1. 函数超过 10 行时必须有中文 DartDoc，说明业务原因。
2. 禁止超过 3 层 `if-else` 嵌套，优先使用卫语句。
3. 变量名必须表达业务含义，禁止 `data`、`flag`、`tmp` 这类宽泛命名。
4. 日期统一使用 `yyyy-MM-dd`。

## 本地验证

```bash
dart pub get
dart analyze
dart test
```
